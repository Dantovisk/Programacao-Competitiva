#include <bits/stdc++.h>

using namespace std;
const int MAX = 2e3+ 15;

#define ll long long

ll dp[MAX][MAX];
int tin[MAX], tout[MAX];

int main(){
    int n, m, d, x;
    cin>>n>>m>>d>>x;
    
    for(int i = 1; i<=n; i++){
        cin>>tin[i]>>tout[i];
    }

    return 0;
}
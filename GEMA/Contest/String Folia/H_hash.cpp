//da pra resolver fazendo o hash de todos os sufixos
//crime [0], rhyme[1], time[2]
//mp["e"] = 0, 1
//mp["me"] = 0, 1
//mp["ime"] = 0, 2
//mp["rime"] = 0, 2
//mp["crime"] = 0

#include <bits/stdc++.h> //incompleto
#define int long long

using namespace std;

const int M1 = 1e9+7;
const int M2 = 1e9+9;

void hashing(int mod, int h[]){

}

void gethash(){

}


int main(){
    ios_base::sync_with_stdio(false);
    cin.tie(NULL);

    string line;

    getline(cin, line);
    while(!line.empty()){
        string a = line;
        reverse(a.begin(), a.end());


        getline(cin, line);
    }


    getline(cin, line);
    while(!line.empty()){
        string a = line;
        reverse(a.begin(), a.end());


        getline(cin, line);
    }


    return 0;
}
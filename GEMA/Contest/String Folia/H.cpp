#include <bits/stdc++.h>
const int MAXN  = 250010 * 30;

using namespace std;

//
int trie [MAXN][27];
int trieT [MAXN][27];

int main(){
    string line; 

    //criando a trie com base nas palavras
    getline(cin, line);
    while(!line.empty()){
        
        
        getline(cin, line);
    }

    //buscando no a rima na trie
    getline(cin, line);
    while(!line.empty()){
        
        getline(cin, line);
    }
    
    

    return 0;
}
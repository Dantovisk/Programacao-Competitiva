#include <bits/stdc++.h>

using namespace std;
using matrix = vector<vector<int>>;

const int mod = 1e9+7;

int main(){
    int n, k;
    cin>>n>>k;

    matrix mt(n, vector<int>(n,0));
    matrix base(n, vector<int>(n,0));

    vector <int> v(n);

    for(int i = 0; i< n; i++){
        v[i];
    }

    return 0;
}